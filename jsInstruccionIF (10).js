function mostrar()
{
	//Genero el número RANDOM entre 1 y 10 
/*  var n1 = 1;
    var numeroAleatorio = Math.round(Math.random(n1)) * 10;                          ESTE TAMBIÉN FUNCIONA.
    */
    var numeroAleatorio = Math.floor(Math.random()) * 10 + 1;

    if (numeroAleatorio >= 9 && numeroAleatorio <= 10) {

        alert("EXCELENTE");

    }
    else if (numeroAleatorio >=) {

    }
	

}//FIN DE LA FUNCIÓN